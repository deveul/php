SELECT UPPER(last_name) AS NAME, first_name, price FROM (SELECT member.id_sub, member.id_user_card, subscription.price FROM member, subscription WHERE member.id_sub = subscription.id_sub AND subscription.price > 42) AS temp, user_card WHERE temp.id_user_card = user_card.id_user ORDER BY last_name, first_name;
